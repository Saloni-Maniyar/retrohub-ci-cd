apiVersion: apps/v1
kind: Deployment
metadata:
  name: retrohub-backend
  namespace: "2401125"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: retrohub-backend
  template:
    metadata:
      labels:
        app: retrohub-backend
    spec:
      imagePullSecrets:
        - name: nexus-creds
      containers:
        - name: retrohub-backend
          image: 127.0.0.1:30085/2401125/retrohub-backend:v1
          ports:
            - containerPort: 5001
